CREATE TABLE users (
  email VARCHAR(30),
  name VARCHAR(30),
  password VARCHAR(30),
  PRIMARY KEY (email)
);

CREATE TABLE usersFoodData (
  foodName VARCHAR(30),
  dayIn Date,
  dayOut Date,
  FOREIGN KEY (email) REFERENCES users(email)
);

CREATE TABLE foodReference (
  foodName VARCHAR(30),
  expiryDate int 100,
  PRIMARY KEY (foodName)
);
